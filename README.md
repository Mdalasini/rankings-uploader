# Rankings Uploader

## Setup

1. Install dependencies with uv:
   ```bash
   uv sync
   ```

2. Set environment variables:
   ```bash
   export RANKINGS_URL="https://rankings.com/"
   export POST_URL="https://api.endpoint.com/"
   ```

3. Run the script:
   ```bash
   uv run python main.py
   ```
